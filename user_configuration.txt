kadmin -p admin/admin -w admin -q "addprinc -randkey deitos/user"
kadmin -p admin/admin -w admin -q "addprinc -randkey ramon/user"
kadmin -p admin/admin -w admin -q "xst -k $HOME/current.keytab deitos/user ramon/user"

kinit -kt $HOME/current.keytab ramon/user
kinit -kt $HOME/current.keytab deitos/user

docker image prune -a


spark = SparkSession.builder\
    .config("spark.sql.warehouse.dir","hdfs://master.deitos.network:9000/data/deitos/hive/warehouse")\
    .enableHiveSupport()\
    .getOrCreate()

